# Tuist build artifacts
Tuist/.build/

# Xcode
*.xcodeproj
*.xcworkspace
.build/
DerivedData/

# Swift Package Manager
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Transient reference clones used during migration / archaeology.
# These are never committed; only the narrow adapted copies that live
# directly under ScaleCloud*/ (the real project.yml specs, the hand-edited
# ScaleCloudApp/ScaleCloudApp.xcodeproj/ pbxproj, and tuistbackup/) belong in the repo.
upstream-ref/
