# NextcloudKit Local Override

This folder is reserved for a local/custom build of the `NextcloudKit` Swift package.

To use a custom build instead of the remote package, place your locally compiled or cloned `NextcloudKit` package here and ensure it contains a valid `Package.swift` manifest.

The Xcode project now resolves `NextcloudKit` from this local path.
